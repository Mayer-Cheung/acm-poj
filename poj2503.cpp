#include <iostream>
#include <cstdio>
#include <map>

using namespace std;
map <char*, char*> mp;
int main()
{
	char s1[15], s2[15];
	while (true)
	{
		while (scanf("%s", s1) && s1 != "\n")
		{
			scanf("%s", s2);
			mp[s2] = s1;
		}
		while (scanf("%s", s1) != EOF)
		{
			if 
		}
	}
}